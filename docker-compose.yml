services:
  backend:
    build:
      context: ./servers
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    environment:
      - NEO4J_SERVER_URL=bolt://neo4j:7687  # Use service name instead of host.docker.internal
      - NEO4J_USERNAME=neo4j
      - NEO4J_PASSWORD=supertestovaciheslo  # Match ISIM's password
      - ISIM_API_URL=http://neo4j_automation:5000
    volumes:
      - ./servers/data:/app/data
    networks:
      - resilmesh_network  # Use ISIM's network

  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4201:80"
    depends_on:
      - backend
    networks:
      - resilmesh_network  # Use ISIM's network
